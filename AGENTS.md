# 市场调研工作规范

## 文件对应

- 输入：`Doc/Ideas/` 下的直接子级 `.md` 文件
- 方法：`Doc/Methodologies/` 下的全部 `.md` 文件
- 输出：`Doc/Analysis/`
- 每个 Ideas 文件整体算一个调研对象，不按行拆分；输出报告使用同名文件。例：`Doc/Ideas/税务产品.md` → `Doc/Analysis/税务产品.md`。
- 忽略旧的 `Doc/IDEAS.md`，除非用户明确指定。

## 特殊命令

### `调研`

每次调研完成后，更新项目根目录 `README.md` 中的调研报告索引目录，使其与 `Doc/Analysis/` 下现有报告保持一致；若 `README.md` 不存在，则创建该文件并加入索引目录。

每次用户说“调研”时：读取 `Doc/Ideas/` 下全部 `.md`，综合 `Doc/Methodologies/` 下全部 `.md` 的方法，逐个开展调研。`Doc/Analysis/` 中已有同名报告的 Ideas 直接跳过，不修改、不覆盖；没有报告的才创建同名分析报告。完成后核对一一对应关系，并汇报新建、跳过和未完成的报告。

报告应包含方法文件要求的分析项和具体产出，标明来源，并区分事实、推断和待验证问题。每次新建或更新的报告，最前面必须输出评分结果。

### `全量调研`

读取 `Doc/Ideas/` 下全部 `.md` 和 `Doc/Methodologies/` 下全部 `.md`，按照完整方法重新研究每个 Idea，并覆盖更新所有同名 `Doc/Analysis/` 报告，不跳过已有报告。每份报告最前面必须输出评分结果；完成后更新 README 索引，核对 Ideas 与 Analysis 的对应关系，并汇报更新、未完成和待验证内容。

### `优化方法论`

每次用户说“优化方法论”时：读取 `Doc/Methodologies/` 下全部 `.md`，调用当前可用的市场调研相关 skill，整体检查并优化现有方法论。保留有效内容，去除重复和冲突，补充必要步骤，提高可执行性，并将修改写回对应文件；若没有可用 skill，则说明原因后按同样目标执行。完成后汇报修改内容。
